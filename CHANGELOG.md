# 🌊 Subnautica 2 Progression & Developer Telemetry Changelog

Master chronological ledger recording gameplay coaching sessions, version-controlled remote snapshots (`192.168.0.100`), flat vault backups (`backups/`), and live binary SaveGame inspections (`savegame_1.sav`). Organized in reverse-chronological order (newest milestones at the top, oldest historical chat sessions at the bottom).

| Date & Time | Milestone / Task | Implementation Summary | Verification Status |
| :--- | :--- | :--- | :--- |
| **2026-06-21 18:57:00** | Dedicated Repository Migration (`subnautica-2`) | Relocated all Subnautica 2 telemetry scrapers, Makefiles, flat backup vaults (`backups/`), and markdown guides out of the parent `pc-build` homelab workspace into a standalone dedicated repository `subnautica-2`. Re-configured internal relative path constants across `subnautica_scraper.py`, `decode_sav.py`, and `sync_remote_vault.py`. | **Verified**: Standalone toolkit operational in `/Users/jakegarrison/Downloads/projects/subnautica-2`. |
| **2026-06-21 16:08:48** | Flat Backups Vault & Multi-Save Decoder | Flattened the local backup vault (`backups/`) to house binary `.sav` files, decoded markdown inspection dumps (`savegame_1_decoded.md`), engine logs (`Subnautica2.log`), and INI graphics profiles side-by-side. Upgraded `decode_sav.py` to automatically decode all `savegame_*.sav` instances during `make pull`. | **Verified**: Successfully synchronized binary saves and 2 plaintext INIs locally. |
| **2026-06-21 15:52:49** | Chat Archive & Local Log Dump Integration | Replaced raw web chat UI noise with a structured engineering archive (`backups/subnuatica_2_previous_chat.md`). Configured automatic local binary dumping of remote Unreal Engine logs (`Subnautica2.log`) on every report run. | **Verified**: Master coaching guide tracking remote commit `4df49f6`. |
| **2026-06-21 15:47:26** | Remote Git Rollback & Save Protection | Initialized Git repository directly on the remote gaming rig inside `C:/Users/jake/AppData/Local/Subnautica2/Saved/.git/` with custom `.gitignore` blocking heavy `.bak` auto-backups and shader caches. | **Verified**: Pristine baseline commit `4df49f6` guarding `savegame_1.sav`. |
| **2026-06-21 15:45:00** | Telemetry Bridge & Scraper Engine Setup | Created local Makefile toolkit and Python SSH bridge (`subnautica_scraper.py`) executing remote regular expression property scraping against Unreal Engine 5 saves. | **Verified**: Successfully extracted player equipment and inventory telemetry. |
| **2026-06-15 14:20:00** | Historical Chat Session: First Base Shopping List | Early interactive chat coaching session analyzing initial gameplay discovery. Scouted ~239m West into Infected Crevasse near Infected Angel Bloom, mined spiky grey crawlspace veins for Silver ore, gathered starter Titanium/Quartz/Copper, and aligned requirements for starter habitat base construction. | **Historical Baseline**: Documented and archived in `backups/subnuatica_2_previous_chat.md`. |
